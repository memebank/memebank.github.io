{
  "name": "memebank.github.io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:site": "node scripts/check-site.mjs",
    "test": "npm run check:site && npm run build"
  },
  "dependencies": {
    "astro": "^7.1.3"
  }
}
