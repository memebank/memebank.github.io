# memebank.github.io

Astro marketing site for Memebank.

```bash
npm install
npm test
npm run dev
```

GitHub Actions builds the static site and deploys it through GitHub Pages. The publisher bootstrap enables the repository's Pages workflow when GitHub permits it.
